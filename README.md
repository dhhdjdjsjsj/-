# Notepad Android App

Minimal Android notepad application with a single note saved locally using `SharedPreferences`.

## Build
Open the project in Android Studio and run the **app** configuration on an emulator or device.

## Features
- Single note editor.
- Save and clear actions.
- Automatic load of the last saved note.
